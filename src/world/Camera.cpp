#include "world/Camera.h"

// Si decides añadir lógica más compleja (shaking, límites de mapa dinámicos), 
// iría aquí. Por ahora, el .h es suficiente, pero este archivo 
// evita que el Makefile falle al buscar src/world/Camera.o

